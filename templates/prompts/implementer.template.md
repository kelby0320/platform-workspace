You are implementing WI-{{WI_NUMBER}} from state/sprint.md.

Scope:
- Repository: {{REPO_NAME}}
- Only implement what is described in WI-{{WI_NUMBER}}.
- Follow that repository's AGENTS.md strictly.
- Use repos.yaml for path resolution.

When complete:
- Update WI-{{WI_NUMBER}} in state/sprint.md:
  - Mark validation complete.
  - List files changed.
  - Note assumptions or blockers.

Run only the validation specified in WI-{{WI_NUMBER}}.
Do not run stack-level validation.
Stop when WI-{{WI_NUMBER}} is complete.
